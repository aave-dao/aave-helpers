name: Head branch workflow

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    uses: bgd-labs/github-workflows/.github/workflows/foundry-test.yml@bbf41e320c69563d7af81752d403dc63b46b06cd
    secrets:
      RPC_POLYGON: ${{secrets.RPC_POLYGON}}
      RPC_MAINNET: ${{secrets.RPC_MAINNET}}
  release:
    needs: [test]
    uses: bgd-labs/github-workflows/.github/workflows/release.yml@main
