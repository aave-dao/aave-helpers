name: Head branch workflow

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    uses: bgd-labs/github-workflows/.github/workflows/foundry-test.yml@4b1ca79965bd12f0ac705dbd8a885daf3addb69b
    secrets:
      RPC_POLYGON: ${{secrets.RPC_POLYGON}}
      RPC_MAINNET: ${{secrets.RPC_MAINNET}}
  release:
    needs: [test]
    uses: bgd-labs/github-workflows/.github/workflows/release.yml@main
